Phiên bản (ID)	File ảnh hưởng	Nội dung thay đổi / Sửa lỗi	Lý do kỹ thuật (Tại sao sửa)	Trạng thái
vqd_3.1	practice_3/vqd_3/final_clustering.ipynb	Thay thế phương pháp Imputation bằng Drop Missing Values (dropna())	Phương pháp điền khuyết Univariate (Mode/Median) làm chệch phân phối dữ liệu gốc và phá vỡ logic nghiệp vụ nhân khẩu học. Việc dropna đảm bảo giữ nguyên cấu trúc phân phối tự nhiên.	Hoàn thành
| Phiên bản (ID) | File ảnh hưởng | Nội dung thay đổi / Sửa lỗi | Lý do kỹ thuật (Tại sao sửa) | Trạng thái |
| --- | --- | --- | --- | --- |
| vqd_3.2 | practice_3/vqd_3/final_clustering.ipynb | Chuyển đổi logic tiền xử lý đặc trưng đầu vào X sang hướng tạo biến tổng hợp `Married_Spending` để xử lý tương quan mạnh | Thay vì điền khuyết hay loại bỏ độc lập làm mất thông tin, việc kết hợp chéo `Ever_Married` và `Spending_Score` giúp mô hình hấp thụ được mối quan hệ phi tuyến mà không gây đa cộng tuyến. | Hoàn thành |
| Phiên bản (ID) | File ảnh hưởng | Nội dung thay đổi / Sửa lỗi | Lý do kỹ thuật (Tại sao sửa) | Trạng thái |
| --- | --- | --- | --- | --- |
| vqd_3.3 | practice_3/vqd_3/final_clustering.ipynb | Đồng bộ hóa toàn bộ notebook: Khôi phục triệt để logic Drop NA ở Bước 3 và kết hợp đặc trưng Married_Spending ở các bước sau | Lệnh checkout trước đó vô tình khôi phục lại mã nguồn điền khuyết (fillna) cũ của git, cần thực hiện đồng bộ lại toàn bộ quy trình làm sạch và tiền xử lý. | Hoàn thành |
| Phiên bản (ID) | File ảnh hưởng | Nội dung thay đổi / Sửa lỗi | Lý do kỹ thuật (Tại sao sửa) | Trạng thái |
| --- | --- | --- | --- | --- |
| vqd_3.5 | practice_3/vqd_3/final_clustering.ipynb | Tái cấu trúc (Refactor): Tích hợp pipeline tiền xử lý DRY, bổ sung IQR Outlier Clipping, và hỗ trợ khoảng cách L1 (Manhattan) cho K-Medoids Scratch | Giảm thiểu trùng lặp code, xử lý nhiễu của giá trị dị biệt trước khi MinMax scaling, và tối ưu khoảng cách hình học cho các đặc trưng thưa (One-Hot). | Hoàn thành |
| vqd_3.6 | practice_3/vqd_3/final_clustering.ipynb | Tái cấu trúc (Refactor): Tối ưu hóa việc tải dữ liệu (sử dụng globals() check) và đồng bộ hóa thứ tự chỉ mục của tất cả tiêu đề Markdown | Tránh lặp lại hoạt động đọc ghi file CSV không cần thiết khi chạy notebook liên tục và sửa lỗi đánh số thứ tự tiêu đề nhảy cóc/trùng lặp. | Hoàn thành |
| vqd_3.7 | practice_3/vqd_3/final_clustering.ipynb | Đồng bộ KMeans thành K=4, bổ sung chi tiết bán kính phân tán cụm cho KMeans, KMedoids, và DBSCAN guide | Sửa lỗi không đồng bộ số cụm KMeans, hiển thị đầy đủ thống kê bán kính phân tán cụm (Min, Median, 90th, Max) và sửa lỗi vòng lặp in trống ở DBSCAN guide. | Hoàn thành |
| vqd_3.8 | practice_3/vqd_3/final_clustering.ipynb | Phục hồi các cell kiểm tra phân bố nhãn mục tiêu và descriptive statistics bị mất ở Bước 2, đồng bộ lại thứ tự tiêu đề | Khôi phục toàn bộ các cell phân tích phân bố nhãn `Segmentation`, mã code mô tả thống kê bị xóa nhầm, đồng thời sắp xếp và đánh số tuần tự các tiêu đề từ ## 1 đến ## 9. | Hoàn thành |
| vqd_3.9 | practice_3/vqd_3/final_clustering.ipynb | Đồng bộ hóa số liệu phân tích khuyết thiếu và dị biệt ở Bước 3 giữa code output và markdown biện luận | Cập nhật các tỷ lệ khuyết thiếu của Ever_Married, Graduated, Profession, Var_1 và ngưỡng dị biệt của Work_Experience trong các cell markdown biện luận cho khớp chính xác với output chạy thực tế sau khi lọc dữ liệu logic. | Hoàn thành |
| vqd_3.10 | practice_3/vqd_3/final_clustering.ipynb | Đồng bộ hóa số liệu tương quan Pearson và Cramer's V ở Bước 4 (EDA) giữa code output và markdown biện luận | Cập nhật các hệ số tương quan tuyến tính của Age, Family_Size, Work_Experience và các hệ số liên kết Cramer's V trong các cell markdown biện luận cho khớp chính xác với output chạy thực tế sau khi loại bỏ nhiễu và làm sạch dữ liệu. | Hoàn thành |
| vqd_3.11 | practice_3/vqd_3/final_clustering.ipynb | Đồng bộ hóa K-Means K=3 ở Bước 6.1, 6.2, 7.1 và toàn bộ các cell kiểm chứng, đánh giá phía sau | Cập nhật số cụm K-Means từ K=4 về K=3 và khoảng cách tâm cụm trong các cell markdown và code của các bước phân cụm, chân dung, kiểm chứng chéo và đánh giá mô hình để đảm bảo đồng bộ hệ thống. | Hoàn thành |
| vqd_3.12 | practice_3/vqd_3/final_clustering.ipynb | Sửa lỗi IndexError trong `AgglomerativeClusteringScratch._assign_labels_from_z` khi chạy các linkage (single, complete, average, centroid, ward) | Vòng lặp gốc chạy cố định `n_samples - 1` bước nhưng lại break sớm khi không tìm thấy cặp `(u, v)`, dẫn đến `linkage_matrix` ít hàng hơn kỳ vọng → `linkage_matrix[i]` IndexError. Giải pháp: (1) Đổi vòng lặp từ `for step in range(steps)` sang `while len(active_clusters) > n_clusters` để dừng đúng lúc đạt đủ cụm mục tiêu; (2) Thay `_assign_labels_from_z` bằng `_assign_labels_from_active` dùng path-compression Union-Find trên `linkage_matrix` thực tế. | Hoàn thành |
| vqd_3.13 | practice_3/vqd_3/final_clustering.ipynb | Đồng bộ hóa Hierarchical Clustering K=4 → K=3 trong toàn bộ Bước 6.3, 7.2 và các cell kiểm chứng/đánh giá | Chốt phương án Hierarchical K=3 thay vì K=4; cập nhật: (1) Cell 140 vòng lặp khảo sát linkage `n_clusters=3`; (2) Cell 142 công thức Dynamic Cut-off cho K=3; (3) Cell 143 chỉ số `Z[n_sub-4, 2]` và `Z[n_sub-3, 2]` cùng tiêu đề đồ thị; (4) Toàn bộ cell markdown Bước 7.2 (chân dung 3 nhóm thay 4 nhóm); (5) Cell kiểm chứng chéo 168-175 và đánh giá 179-186 đồng bộ nhãn Hierarchical (K=3). | Hoàn thành |
| vqd_3.14 | practice_3/vqd_3/final_clustering.ipynb | Sửa lỗi Dendrogram và Scatter K=3 vẫn hiển thị 4 cụm (Cell 143) | Khi `best_hierarchical` chạy với `n_clusters=3`, thuật toán dừng sớm → `linkage_matrix` chỉ có `n_sub - 3` hàng → `Z[n_sub-3]` IndexError và dendrogram bị cắt ngắn. Giải pháp: train thêm `hc_full` (n_clusters=2) để lấy `Z_full` đầy đủ (`n_sub - 2` hàng) làm nguồn cho dendrogram và dynamic cut-off, trong khi `train_labels_hier` từ `best_hierarchical` (n_clusters=3) vẫn là nguồn nhãn chính xác cho scatter plot K=3. | Hoàn thành |
| vqd_3.15 | practice_3/vqd_3/final_clustering.ipynb | Sửa lỗi Silhouette Score = 0 và tối ưu hóa `custom_silhouette_score` (Cell 99, 103, 111) | Hàm gốc dùng vòng lặp Python `for i in range(n_samples)` với `squareform(pdist(X))` rất chậm và bị lỗi logic khi sub-sample chỉ chứa 1 nhãn duy nhất → trả về 0.0. Viết lại hoàn toàn bằng NumPy vectorized (`X @ X.T`), tính `a_i` bằng `np.ix_` và `b_i` bằng `np.minimum` cho từng cặp nhãn, thêm guard `ab_max == 0 → 1e-12`. Giảm `sample_idx` từ 6132/6000 xuống 2000 để tránh OOM khi tính ma trận khoảng cách 2000×2000. | Hoàn thành |
| vqd_3.16 | practice_3/vqd_3/final_clustering.ipynb | Sửa lỗi gốc NaN toàn bộ pipeline: (1) Cell 81 thiếu `return` trong `fit_transform`; (2) Cell 84 tạo `Married_Spending` trước khi dropna → sinh category `'nan_Low'` | Lỗi 1: `CustomMinMaxScaler.fit_transform()` không có câu lệnh `return` → trả về `None` → gán `X_scaled[numerical_cols] = None` → toàn bộ X_train thành NaN → Inertia = nan, Silhouette = 0, centroids = nan. Lỗi 2: `Ever_Married.astype(str)` trên cột có NaN tạo chuỗi `'nan_Low'` trước khi dropna. Fix: thêm `return self.transform(X)` vào Cell 81; thêm `X = X.dropna().reset_index(drop=True)` trước bước tạo `Married_Spending` ở Cell 84; cập nhật signature `preprocess_pipeline` để trả về cả `y_train`. | Hoàn thành |
| vqd_3.17 | practice_3/vqd_3/final_clustering.ipynb | Sửa lỗi KeyError: 'Segmentation' (Cell 84) phát sinh từ fix vqd_3.16 | `Train_cleaned.csv` không chứa cột `Segmentation` (đã tách riêng sang `y_train.csv` ở Bước 3). Fix vqd_3.16 sai khi thêm `y = X['Segmentation'].values` vào pipeline. Xóa dòng đó, giữ `preprocess_pipeline` chỉ trả về `(X_scaled, customer_ids)` như thiết kế gốc; `y_train` tiếp tục được load từ `y_train.csv` ở Cell 90. | Hoàn thành |
